<div class="postlist wa-stack"  class=".wa-gap-xxl" size="large">
	{% for post in postslist | reverse %}
	<div class="wa-flank postlist-item{% if post.url == url %} postlist-item-active{% endif %}">

		<div class="wa-flank wa-gap-xs wa-align-items-start">
			{% if post.data.cover %}
			<div>
				<a href="{{ post.url }}">
					{% image 'books' + post.data.path + '/' + post.data.cover, post.data.title, "120px" %}
				</a>
			</div>
			{% endif %}
		<div class="postlist-item-header" style="width: 300px">
			{% if post.data.author %}
			  <a href="/authors/{{ post.data.author | customSlugify }}/">{{ post.data.author }}</a>
			{% endif %}:
			<a href="{{ post.url }}" class="postlist-link">{% if post.data.title %}{{ post.data.title }}{% else %}<code>{{ post.url }}</code>{% endif %}</a>
			<span class="read-date" style="white-space: nowrap;">(&nbsp;<time class="postlist-date" datetime="{{ post.date | htmlDateString }}">{{ post.date | germanDate }}</time>&nbsp;)</span>
		</div>
		</div>
					<div class="wa-size-l">
				{{ post.templateContent | excerpt | safe }}
			</div>
	</div>
	{% endfor %}
</div>
